<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fdgproject.firedge.myinmobiliaria" >

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.fdgproject.firedge.myinmobiliaria.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.fdgproject.firedge.myinmobiliaria.Formulario"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.fdgproject.firedge.myinmobiliaria.ActividadDetalle"
            android:label="@string/app_name" >
        </activity>
    </application>

</manifest>
